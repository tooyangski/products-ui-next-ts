## Getting Started

First, Clone and run express-ts-api using `yarn dev`. This is required to run the backend.
Second, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
